engine: FHIRConnect/v0.0.1
type: model
metadata:
  name:  laboratory_test_analyte
  version: 0.0.1a # version of this particular mapping
spec: # schema specific to the FHIRConnect v0.0.1 engine
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-CLUSTER.laboratory_test_analyte.v1

mappings:
  - name: "status"
    with:
      fhir: "$resource.status"
      openehr: "$archetype/items[at0005]"

  - name: "issued"
    with:
      fhir: "$resource.issued"
      openehr: "$archetype/items[at0006]"

  - name: "analyteMeasurement"
    with:
      fhir: "resourcePath.value"
      openehr: "$archetype/items[at0001]"

  - name: "analyteName"
    with:
      fhir: "resourcePath.code"
      openehr: "$archetype/items[at0024]"

  - name: "interpretation"
    with:
      fhir: "resourcePath.code"
      openehr: "$archetype/items[at0004]"

  - name: "testmethod"
    with:
      fhir: "resourcePath.method"
      openehr: "$archetype/items[at0028]"

  - name: "specimen"
    with:
      fhir: "$resource.specimen"
      openehr: "$reference"
    reference:
      resourceType: "Specimen"
      mappings:
        - name: "identifierInReference"
          with:
            fhir: "$fhirRoot.identifier"
            openehr: "$archetype/items[at0026]"
        - name: "specimenLink"
          with:
            fhir: "$fhirRoot"
            openehr: "$archetype/items[at0026]/links" # will use link
            type: "REFERENCED_MAPPING" # execute mapping or use already mapped one to get data
            openehrLink: #follow this link
              meaning: "link to the ehr object this specimen was mapped from"
              type: "specimen"

  - name: "comment"
    with:
      fhir: "resourcePath.note"
      openehr: "$archetype/items[at0057]"



